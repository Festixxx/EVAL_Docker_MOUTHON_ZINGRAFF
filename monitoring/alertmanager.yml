global:
  resolve_timeout: 5m

route:
  receiver: "console"
  group_by: ["alertname"]
  group_wait: 10s
  group_interval: 5m
  repeat_interval: 1h

receivers:
  - name: "console"
    # Récepteur par défaut : log dans Alertmanager uniquement.
    # Pour activer les emails, décommentez et remplissez :
    # email_configs:
    #   - to: "votre@email.com"
    #     from: "alertmanager@arcade.local"
    #     smarthost: "smtp.gmail.com:587"
    #     auth_username: "votre@email.com"
    #     auth_password: "mot_de_passe_app"
